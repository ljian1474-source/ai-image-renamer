{
  "name": "ai-image-renamer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "一个只做图片识别并自动改名的极简网站",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
